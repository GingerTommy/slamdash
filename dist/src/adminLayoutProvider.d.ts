declare class AdminLayoutProvider implements LayoutProvider {
    private cells;
    getNamedLayout(name: string, cells: Array<CellInfo>): string;
    private getChartWrapper(index);
    private sixTwoTwo();
}
export { AdminLayoutProvider as adminLayoutProvider };
